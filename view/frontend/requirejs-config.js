var config = {
    paths: {
        'logik-configurator': 'Logik_Integration/js/logik-configurator'
    },
    shim: {
        'logik-configurator': {
            deps: ['jquery', 'ko']
        }
    }
}; 